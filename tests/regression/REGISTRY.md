# Regression Registry

Living specification of the product. Every regression test appears here with the requirement or defect that justifies it. Tests are never removed for convenience; a changed expectation needs a `kind: test-change` decision in `.agent/decisions/`.

| Test file :: test name | Origin unit | Behavior preserved | Notes |
|---|---|---|---|
| `tests/regression/readme-behavior.test.js` :: README live trail shows the whole expression 4+8+9 while typing | BOOT-001 | README: the main line shows the growing expression while typing (`4`, `4+`, `4+8`, `4+8+`, `4+8+9`), expression line stays empty | Driven through real `index.html` + page scripts via `tests/helpers/dom-stub.js` |
| `tests/regression/readme-behavior.test.js` :: README equals shows the expression on the small line and the result below | BOOT-001 | README: `=` splits the display into expression `4+8+9` (small line) and result `21` | |
| `tests/regression/readme-behavior.test.js` :: README chained calculations evaluate left to right | BOOT-001 | README: chained operations, left to right, no precedence (`2+3×4=20`, `8−4−2=2`) | Operator glyphs are U+00D7 / U+2212 |
| `tests/regression/readme-behavior.test.js` :: README operator right after equals continues from the result | BOOT-001 | README: an operator after `=` continues from the result (`4+8+9=` then `+5=` gives `21+5` / `26`) | |
| `tests/regression/readme-behavior.test.js` :: README decimal point support adds decimal operands | BOOT-001 | README: decimals (`1.5+2.25=3.75`) | |
| `tests/regression/readme-behavior.test.js` :: README floating-point rounding trims noise and limits decimals | BOOT-001 | README: `0.1+0.2` shows `0.3`; results limited to ten decimal places (`1÷3` gives `0.3333333333`) | |
| `tests/regression/readme-behavior.test.js` :: README division by zero shows Error | BOOT-001 | README: `5÷0=` shows `Error` (division by zero as the last step only; the mid-chain path is owned by `CALC-001` and deliberately not pinned here) | |
| `tests/regression/readme-behavior.test.js` :: baseline Error recovery starts a fresh calculation on the next digit | BOOT-001 | Historical baseline (README silent): a digit after `Error` starts a brand-new calculation (`5÷0=` then `7` gives `7`, expression line empty) | Baseline behavior at `e02035b`; changing it needs a `kind: test-change` decision |
| `tests/regression/source-safety.test.js` :: shipped scripts contain no import or export statement | BOOT-001 | Classic scripts only: `script.js` and `calculator-core.js` use no `import`/`export`/dynamic `import()` (README `file://` promise, CLAUDE.md section 1); `module.exports` and the `globalThis` guard stay allowed | Static scan, comments stripped; RED until `calculator-core.js` exists (a missing file fails, never skips) |
| `tests/regression/source-safety.test.js` :: index.html contains no script of type module | BOOT-001 | `index.html` loads only plain `<script src>` tags, never `type="module"` (would break `file://`) | Scans all three shipped files first, so it is RED until the core exists |
| `tests/regression/source-safety.test.js` :: shipped scripts contain no eval, new Function or document.write | BOOT-001 | No dynamic code execution in the shipped scripts (CLAUDE.md section 4 security) | Pattern `\beval\b` is a superset of `eval(`, so an indirect `const f = eval;` is caught too. **Widened by KEY-001 (D-009)**: now also scans `index.html`'s raw markup (HTML comments stripped), closing the gap where an inline `onclick="eval(x)"` attribute previously passed unnoticed (`BOOT-001-sec1` F-1) |
| `tests/regression/source-safety.test.js` :: shipped scripts contain no innerHTML, outerHTML or insertAdjacentHTML | BOOT-001 | Display is written with `textContent` only, never markup (CLAUDE.md section 4 security) | Complements the runtime markup-write traps in `tests/integration/dom-click.test.js`. **Widened by KEY-001 (D-009)**: now also scans `index.html`'s raw markup (HTML comments stripped), same gap as the row above |
| `tests/regression/source-safety.test.js` :: index.html and scripts reference no third-party or CDN host | BOOT-001 | No third-party scripts or CDNs (CLAUDE.md section 4): every `src`/`href` is relative; no absolute or protocol-relative URL in the three shipped files | Fails if `index.html` has no `src`/`href` at all (guards against a vacuous scan). **Widened by KEY-001 (D-009)**: `index.html` is now scanned with the full `externalHosts` pattern set (also the protocol-relative `//host` pattern), not only the absolute-URL pattern, closing the gap where a bare `//evil.example.com` outside a `src`/`href` attribute previously passed unnoticed (`BOOT-001-sec1` F-1) |
| `tests/regression/source-safety.test.js` :: stripComments does not hide code following a regex literal containing a double slash | KEY-001 | The static security scan's own comment-stripper does not mistake a regex literal's escaped `//` (e.g. `/https?:\/\//`) for a line comment, which would otherwise discard a same-line `eval(`/`innerHTML=` that follows it (`BOOT-001-sec1` F-2) | Calls `stripComments` directly on a crafted snippet, no shipped file involved. Defect found in `BOOT-001-sec1`; fixed by `KEY-001` (D-009) |
| `tests/regression/divide-by-zero.test.js` :: README division by zero shows Error also when an operator resolves it | CALC-001 | README: division-by-zero shows `Error` as soon as the division is evaluated; `5 ÷ 0 +` shows expression `5÷0` and `Error`, the pressed operator is discarded (same two lines as `5 ÷ 0 =`) | Real page through `tests/helpers/dom-stub.js`. Defect found in BOOT-001 gate 2 (OQ-B1): the unfixed page showed `5÷0+` |
| `tests/regression/divide-by-zero.test.js` :: README division by zero shows Error also in the middle of a longer chain | CALC-001 | README: `2 + 3 ÷ 0 ×` shows expression `2+3÷0` (the whole typed chain, OQ-C1) and `Error` | Real page. Companion of the BOOT-001 row that pins the `=` form |
| `tests/regression/divide-by-zero.test.js` :: README Error recovery works after a divide-by-zero resolved by an operator | CALC-001 | After `5 ÷ 0 +`: `=` is ignored (still `5÷0` / `Error`, never `NaN`) and a digit starts a fresh calculation (`7`, expression line empty) | Real page. Asserts the `Error` state before any recovery input, because a digit after `Error` already started fresh before the fix |
| `tests/regression/divide-by-zero.test.js` :: README divide-by-zero bullet says Error shows as soon as the division is evaluated, also mid-chain | CALC-001 | README states the immediate-`Error` behavior: the divide-by-zero bullet names `Error`, the example `5 ÷ 0 +`, chains, and "as soon as" / "immediately" / "at once" | Static read of `README.md`, no page. Red until the documenter edits the README bullet (D-006); a missing bullet fails, never skips |
