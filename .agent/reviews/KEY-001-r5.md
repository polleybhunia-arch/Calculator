---
unit: KEY-001
reviewer: reviewer
model_attested: claude-opus-5
reviewed_ref: 8c36740af901c680ee4305fcbb0684222201777d
verdict: APPROVED
cycle: 5
---

# Review KEY-001 r5 — verdict: APPROVED

Running on **claude-opus-5** (no reviewer fallback). Full re-review of
`f9426e284219f49816abad088275d219c72bdd81..8c36740af901c680ee4305fcbb0684222201777d`, not a diff
spot-check of the one new test. I did not read handoffs `KEY-001-04`, `-06`, `-08`, `-16`, `-24`,
`-32`, `-40`, nor any implementer/integration-tester justification.

**Summary.** My cycle-4 finding **F-1 is closed, correctly and minimally**. One integration row was
added (`tests/integration/keyboard.test.js`, +22 lines), no product file changed, and I reproduced
all three named mutations myself in a throwaway worktree: each kills **exactly one** test — the new
one — and each failure signature matches the wrong behavior I predicted at r4 to the character
(M29 `'123'`, M27 `'123'`, M31 `'0'`, against the expected `'12'`). All four gates are green at this
SHA when I run them. All eight ACs are SATISFIED; I drove every one of them directly against the
shipped code rather than inferring from the suite.

Answer to the dispatch's explicit question (is the `button-type × key × repeat` space now
exhaustively covered?): **yes for every naturally reachable mutation, and I recommend no sixth
test-only cycle.** Every mutation in this family that a plausible refactor actually produces —
narrowing the branch or the repeat guard by key (`Enter`-only), by attribute (`data-number` /
`data-operator` / `data-action` dropped from `isCalculatorButton`), or by button family (skip digit /
operator / action) — is now killed by a named row. The three survivors I found this cycle all
require a **single `data-*` value** carve-out (`action !== 'clear'`, `action !== 'equals'`, or a
`Space`-and-non-digit conjunction), which no realistic simplification of this four-line branch
produces; two of them I measured to be behaviorally **equivalent**. I record them as Nits (F-3, F-4),
not as another blocking cycle. If the user wants this space closed *by construction* instead of by
enumeration, the proportionate structural change is in F-3's note — one table-driven row over every
button in `index.html` × `{Enter, Space}` × `{repeat, non-repeat}` with the existing
`spyOnDispatches` count oracle, replacing the four hand-written per-family rows. That is a test-side
change; I do **not** recommend `D-013`'s rejected "take activation over entirely" alternative, which
would trade a stub-verifiable guarantee for an unverifiable real-browser one.

## Acceptance criteria verdicts
- AC-1: SATISFIED — I drove `4 + 8 + 9 Enter` through synthetic `keydown`s (`E="4+8+9" C="21"`) and the same sequence through clicks (`{"expression":"4+8+9","current":"21"}`) — identical. Pinned by integration `typing four plus eight plus nine then Enter renders the same result as the equivalent clicks` and regression `keyboard chain four plus eight plus nine Enter matches the same sequence performed with clicks`; probe M24 (`modifiersOf` returns all-false) and M26 (`isCalculatorButton` always true) each turn key-path rows red, so the path is genuinely exercised.
- AC-2: SATISFIED — driven directly: `0 . 1 + 0 . 2 =` → `C="0.3"`; `1 . 2 . 3` → `C="1.23"` (the second `.` is ignored). Pinned by `typing a decimal chain through keys renders zero point three like the click path`, `a second decimal point key within the same number is ignored through the keyboard`, `the comma key is accepted as an alternate decimal point through the keyboard`; probe C6 (drop the `','` entry) → red.
- AC-3: SATISFIED — driven directly for all six keys after `9`: `*`→`9×`, `x`→`9×`, `X`→`9×`, `/`→`9÷`, `-`→`9−`, `+`→`9+`. Pinned by `each operator key including x X and the slash key renders the matching display symbol` (whole-pair equality) and unit `maps each operator key including the letter x and its capital…`; the entries derive from the core's single `OPERATORS` source, and probes C3/C6/C7 show a deleted or re-pointed `KEY_MAP` entry is caught at both layers.
- AC-4: SATISFIED — driven directly: `1 2 3 Backspace` → `C="12"`; `4 + 8 Escape` → `E="" C="0"`; `5 / 0 + Delete` (from `Error`) → `E="" C="0"`. Pinned by `the Backspace key removes the last typed digit like the DEL button`, `the Escape key clears the display from an unfinished chain and from an Error`, `the Delete key also clears the display like Escape`; probes C7 (`Backspace`→`clear`) and C3 (drop `Delete`) both red.
- AC-5: SATISFIED — driven directly with a counting `preventDefault`: `a`, `F5`, `Tab`, `ArrowLeft`, `Space`, `Ctrl+r`, `Meta+/`, `Alt+5` each leave the display at `7` with `preventDefault=0`; a mapped modifier-free `5` gives `preventDefault=1` exactly; `Shift+*` still maps. Pinned by the five AC-5 integration rows plus regression `Ctrl+R is inert…`; probes M15 (drop `preventDefault()` for mapped keys) and M24 are each killed. The `Space`-on-a-focused-button carve-out (my r3 F-3) is written into AC-5 and `D-013` clause 4 and is pinned indirectly by M2 (guard returns without `preventDefault()`) → 4 red.
- AC-6: SATISFIED — both halves driven directly. Mouse half: after `page.click(buttonFor('4'))`, `document.activeElement` is `null`, and `click 4` then `+ 8 Enter` renders `E="4+8" C="12"` (the original `4+84` defect is gone); probes M3 (always blur) and M4 (never blur) each kill the row. `Tab` half: I exercised **all five** button families × `{Enter, Space}` after `123` — `7`→`1237`, `+`→`123+`, `=`→`123`, `AC`→`0`, `DEL`→`12`, each exactly one action, focus retained in every case. Now pinned for digit, operator **and** action buttons (M30 → 3 red, M28 → 1 red, M27/M29 → 1 red each via the new row, M5 removing the whole branch → 5 red, up from 4 at r4).
- AC-7: SATISFIED — driven directly: held digit `5` → `555`; held `Backspace` from `123` → `1`; held `+` after `9` → `9+` (once); held `Enter` after `4+8` → `E="4+8" C="12"` (once); held `.` then held `4` → `123.4`. Pinned by the four document-level repeat rows plus the four focused-button rows (`holding Enter … digit button`, `holding Space … digit button`, `holding Enter … operator button`, and the new `the DEL button reached by Tab and activated by a held Enter deletes exactly once`). Probes C1 (`allowsRepeat` always true) → 2 red, C2 (true for any action) → 1 red, M6 (drop the `!allowsRepeat` early return) → 2 red, M1/M2 → 4 red each, M11 (repeat guard narrowed to digit buttons) → 2 red (was 1 at r4 — the new row strengthens it), M31 → 1 red. **My r4 survivor M31 is dead.**
- AC-8: SATISFIED — `README.md:16` Features bullet plus `## Keyboard` (`:18-29`) documents the map key for key against the shipped `KEY_MAP`: digits `0`–`9`, `.`/`,`, `+ - * /` with `x`/`X` alternates, `Enter`/`=`, `Backspace`, `Escape`/`Delete`, the auto-repeat policy and the modifier policy. I compared it line by line with `calculator-core.js:209-220`; no key documented that is not mapped, none mapped that is not documented. Regression `README documents every mapped key with its calculator action` and `README Features lists keyboard support` pass in my own run.

## What I ran myself
Scope. `git rev-parse HEAD` in the repo = `ee66c6077b6c79d5aeb00ff7ae2041a65c84326f`, which is **not**
`head_ref` `8c36740a…` (F-6). `git diff --name-status 8c36740a..HEAD` is `.agent/`-only (3 dispatch
handoffs, `state.md`, the 4 `latest-*-final.json` pointers, `units/KEY-001.md`), so no product or test
file differs. I therefore ran everything in a **scratch git worktree checked out at `8c36740a`** under
the session scratchpad (`git rev-parse HEAD` there = `8c36740a…`, `git status` clean), and removed it
afterwards — `git worktree list` now shows only the repo, and the repo's SHA-bound
`latest-*-final.json` pointers were never rewritten by my runs.

I also confirmed `head_ref` is a safe binding: `git diff --name-status ae9d7c8..8c36740a -- . ':!.agent'`
is **empty**, i.e. `8c36740a` (a handoff commit) carries byte-identical product and test content to
`ae9d7c8` (the commit that actually added the test), so the evidence bound to `8c36740a` describes the
right code.

`git diff --name-status 377024ba..8c36740a` (cycle-4 → cycle-5 delta) touches exactly one non-`.agent`
file: `tests/integration/keyboard.test.js` (+22 lines, one new `test(...)` block, nothing removed or
edited). `script.js`, `calculator-core.js`, `index.html`, `style.css`, `README.md`, `tests/helpers/**`
and every other test file are byte-identical to the cycle-4 head. Over the whole unit
(`f9426e28..8c36740a`, non-`.agent`): 3 files modified (`README.md`, `calculator-core.js`, `script.js`),
4 test/helper files added, 3 pre-existing test/helper files modified (`tests/helpers/dom-stub.js`,
`tests/helpers/source-scan.js`, `tests/regression/source-safety.test.js` — each covered by the
`D-008`/`D-009` test-change records; `validate.mjs state` = `OK` confirms), **nothing deleted**.

Gates, re-run by me through the recorder at `8c36740a` (worktree):

| Gate | Command | My result at `8c36740a` | Recorded evidence |
|---|---|---|---|
| unit | `node .agent/tools/run-gate.mjs unit --unit KEY-001` | exit 0, tests 83, pass 83, fail 0, `dirty=false`, `head=8c36740a` | `latest-unit-final.json` 83/83 at `8c36740a` — matches |
| integration | `node .agent/tools/run-gate.mjs integration --unit KEY-001` | exit 0, tests 50, pass 50, fail 0 | `latest-integration-final.json` 50/50 — matches |
| regression | `node .agent/tools/run-gate.mjs regression --unit KEY-001` | exit 0, tests 25, pass 25, fail 0 | `latest-regression-final.json` 25/25 — matches |
| lint | `node .agent/tools/run-gate.mjs lint --unit KEY-001` | exit 0, PASS | `latest-lint-final.json` PASS — matches |

No discrepancy with the claimed counts. `node .agent/tools/validate.mjs state` → `state: OK`, exit 0.
`grep` for `.only(` / `.skip(` / `todo:` under `tests/` → no hits.

**Mutation probes** — 39 mutations, run against integration+regression (75 tests) via the stub's
in-memory `CALC_STUB_TRANSFORM` hook, plus one core mutation applied as a file edit inside the
throwaway worktree and reverted (verified with `git status`). Nothing on disk in the repo was
modified. Note for anyone reproducing this: `script.js` and `calculator-core.js` use **CRLF** line
endings, so a multi-line `find` string written with `\n` throws the stub's occurrence-count error and
produces a cascade of ~65 unrelated failures that looks like a code defect but is a probe-spec error.
I hit this on four probes and corrected them; the corrected results are below. This is the same trap
the dispatch reports, and it is the reason a bare "the mutation failed the suite" claim is not
evidence.

| # | Mutation (`script.js` unless noted) | Outcome |
|---|---|---|
| M0 | none (baseline) | 75/75 green |
| **M29** | `isCalculatorButton` forgets `data-action` (r4 F-1) | **killed — exactly 1**: `the DEL button reached by Tab and activated by a held Enter deletes exactly once`, actual `'123'` vs expected `'12'` |
| **M27** | branch skips action buttons (r4 F-1) | **killed — exactly 1**: same row, actual `'123'` |
| **M31** | repeat guard skips action buttons (r4 F-1) | **killed — exactly 1**: same row, actual `'0'` |
| M1 | delete the whole `if (event.repeat) {…}` guard | killed (4 — was 3 at r4) |
| M2 | guard returns without `preventDefault()` | killed (4 — was 3) |
| M3 / M4 | `event.detail > 0` → always / never blur | killed (1 each) |
| M38 | `event.detail > 0` → `>= 0` | killed (1) |
| M5 | remove the whole `Enter`/`Space`-on-button branch | killed (5 — was 4) |
| M6 | drop the `!allowsRepeat(input)` early return | killed (2) |
| M10 | repeat guard narrowed to `event.key === 'Enter'` | killed (1) |
| M11 | repeat guard narrowed to digit buttons | killed (2 — was 1) |
| M15 | remove `preventDefault()` for mapped keys | killed (1) |
| M17 | branch condition narrowed to `Enter` | killed (1) |
| M24 | `modifiersOf` returns all-false | killed (1) |
| M26 | `isCalculatorButton` always `true` | killed (6) |
| M28 | branch skips operator buttons | killed (1) |
| M30 | branch skips digit buttons | killed (3) |
| M32 | repeat guard skips operator buttons | killed (1) |
| M33 | `isCalculatorButton` forgets `data-operator` | killed (1) |
| M34 | `isCalculatorButton` forgets `data-number` | killed (3) |
| M39 | repeat guard skips `data-action="delete"` only | killed (1) |
| C1 | `allowsRepeat` always `true` | killed (2) |
| C2 | `allowsRepeat` true for any action | killed (1) |
| C3 | drop the `Delete` entry from `KEY_MAP` | killed (1) |
| C6 | drop the `','` entry | killed (1) |
| C7 | `Backspace` maps to `clear` | killed (2) |
| C4 | `mapKey` stops checking `altKey` | survives integration+regression; **killed at the unit layer** (file-edit probe: `returns null for a mapped key held with Ctrl Meta or Alt…`, 1 fail of 83) — the already-recorded r1 F-6 known issue, not a new gap |
| **M40** | branch skips `data-action="clear"` only | **SURVIVED** 75/75 (F-3) |
| **M36** | repeat guard skips `Space` on non-digit buttons | **SURVIVED** 75/75 (F-4) |
| M35 | repeat guard skips `data-action="equals"` only | SURVIVED — **equivalent mutant**, measured |
| M37 | branch skips `data-action="equals"` only | SURVIVED — **equivalent mutant**, measured |
| C5 | `mapKey` returns the shared map entry instead of a copy | SURVIVED (carried r4 F-4, Nit) |

Measured behavior of the four survivors against the shipped code (stub, in memory):

```
scenario                                   SHIPPED   M40      M35      M37      M36
Tab→AC,  single Enter (after 4+8)          ""/"0"    "4+8"/"12"  ""/"0"   ""/"0"   ""/"0"
Tab→AC,  held   Enter (after 4+8)          ""/"0"    "4+8"/"12"  ""/"0"   ""/"0"   ""/"0"
Tab→AC,  single/held Space (after 4+8)     ""/"0"    ""/"0"   ""/"0"   ""/"0"   ""/"0"
Tab→DEL, single Enter (after 123)          ""/"12"   ""/"12"  ""/"12"  ""/"12"  ""/"12"
Tab→DEL, held   Space (after 123)          ""/"12"   ""/"12"  ""/"12"  ""/"12"  ""/"0"
Tab→=,   single/held Enter, held Space     "4+8"/"12" identical in all four mutants
Tab→+,   single Enter / held Space          ""/"9+"  identical in all four mutants
```

So M35 and M37 are **provably equivalent** for every input I could construct — `Enter` maps to
`equals` anyway, and a repeated `equals` after a result is idempotent in this core. They are not
findings, and they confirm my r4 statement that the guard is degenerate for the `=` button. M40 and
M36 are behaviorally distinguishable and are recorded as F-3/F-4.

## Findings
| ID | Severity | Location (file:line) | Problem | Required change |
|---|---|---|---|---|
| F-1 | Minor | `.agent/decisions/D-013-native-activation-is-the-browser-channel.md:122-126` (Consequences) | The dispatch asked me to confirm `D-013`'s corrected case-set table rather than assume it. **The clause-3 table (`:69-75`) is now accurate** — I checked each of its seven rows against the code and the test file, and the new `Enter`/repeat/action-button row correctly names `the DEL button reached by Tab and activated by a held Enter deletes exactly once`; the trailing paragraph (`:77-81`) about `Space` on operator/action buttons and the missing matrix entries is also still true. But **Consequences is stale**: it still lists as open follow-ups (a) "the unpinned held-`Space` cell (`arch3` F-1, one integration row ≈16 lines)", closed in cycle 3 and named in the table two lines above, and (b) the manual-checklist line "hold `Space` on a `Tab`-focused button", which is now `RK-3` item 4 in `.agent/units/KEY-001.md:151-153`. This is the *third* sub-item of my r4 F-2, the only one not carried out. A reader who trusts Consequences over the table will re-open closed work. | Strike those two items from `D-013` Consequences, leaving only the genuinely open one (the missing matrix rows, my F-2). Decision records are `architecture-reviewer`/human-writable, not mine. |
| F-2 | Minor | `.agent/units/KEY-001.matrix.md` (AC-6/AC-7 tables) | The matrix names rows that all exist (`validate.mjs state` = OK, which checks matrix→test), but **none of the four review-driven integration tests has a matrix row**: `holding Enter on a Tab focused digit button performs the action once` (r2), `holding Space … digit button …` and `holding Enter … operator button …` (r3), and the new `the DEL button reached by Tab and activated by a held Enter deletes exactly once` (r4). I verified this mechanically against the matrix source. `validate.mjs` cannot catch it because it only checks the matrix→test direction. Carried since r4 F-3 and now one row worse; the most defect-dense quadrant of AC-6/AC-7 — five review cycles of findings — is traceable only through review files. | `test-designer` adds four rows to the AC-6/AC-7 tables with the exact test names, key sequences and expected results, and refreshes the stale `preventDefault` note the unit's Known Issues already flags. No test-code change. |
| F-3 | Nit | `tests/integration/keyboard.test.js` (AC-6 block); guarantee at `script.js:89` | Probe **M40** (`isCalculatorButton(event.target) && event.target.dataset.action !== 'clear'`) survives all 75 integration+regression tests while producing user-visible wrong behavior: after `4 + 8`, `Tab`→`AC`→`Enter` renders `4+8` / `12` (it *evaluates*) instead of `""` / `0`. This is precisely the "optional second row for `AC`" my r4 F-1 named and that was not added. I rate it **Nit, not Major**: every mutation in this family that a realistic refactor produces is now dead (M27, M29, M31, M11, M28, M30, M33, M34, M17, M10), and M40 requires hand-writing a carve-out for one specific `data-action` value. The shipped behavior is correct — I measured it. | Either (a) one row `the AC button reached by Tab and activated by Enter clears instead of evaluating`: `typeKeys(page, '4 + 8')`, `page.buttonFor('AC').focus()`, one non-repeat `Enter`, assert `display('', '0')`; or (b) better and strictly stronger — replace the four hand-written per-family rows with **one table-driven row** iterating every `button[data-number], button[data-operator], button[data-action]` in `index.html` × `{Enter, Space}` × `{repeat, non-repeat}`, asserting via the existing `spyOnDispatches` oracle that a non-repeat key dispatches exactly once and repeats add nothing. (b) closes this quadrant by construction and ends the per-cycle enumeration; it is a test-only change. |
| F-4 | Nit | `tests/integration/keyboard.test.js` (AC-7 block); guarantee at `script.js:98` | Probe **M36** (repeat guard skips `Space` on non-digit buttons) survives all 75 tests and is behaviorally distinguishable: `Tab`→`DEL`→held `Space` after `123` renders `0` (deletes every repeat) instead of `12`. `Space` is pinned for digit buttons only; operator and action buttons under held `Space` have no row. Already recorded as a Known Issue (r4 F-2/F-3, `arch4` F-1/F-2) and named in `D-013:77-81`; I re-confirm it with a measurement rather than by construction, and note it is subsumed by F-3 option (b). | Covered by F-3 option (b). If option (a) is chosen instead, add the mirror row for held `Space` on `DEL`. |
| F-5 | Nit | `calculator-core.js:225` (comment) and `:232` | Carried from r4 F-4, unchanged: the comment promises "Returns a fresh copy of the descriptor so a caller can never mutate the shared map entry", but probe C5 (`return mapped === undefined ? null : mapped;`) survives all 83+75 tests. No current consumer mutates the descriptor, so no defect today. | Add a unit row (call the map twice for the same key, mutate the first result, assert the second is unchanged) or drop the promise from the comment. |
| F-6 | Nit | repository state | Fifth cycle running, `git rev-parse HEAD` (`ee66c607`) ≠ `head_ref` (`8c36740a`); `.agent/`-only delta, so gates are unaffected, but every reviewer must first prove the drift is inert before any result means anything. Repeat of r4 F-5 / r3 F-5 / r1 F-8. Related and worth noting: `head_ref` points at a *handoff* commit (`8c36740a`), not the commit that added the test (`ae9d7c8`); harmless here — I verified the two are identical outside `.agent/` — but it is a second way the binding can drift from the code it is supposed to name. | Unchanged recommendation, human decision: make `head_ref` the branch's last commit, or have `run-gate.mjs` refuse to rebind a `latest-*-final.json` bound to a different SHA without an explicit flag. The zero-cost reviewer-side mitigation is the scratch worktree at `head_ref` used here and at r4. |

Carried-forward Minor/Nit findings from r1/r2/r3/r4 and the security/architecture reviews were
re-checked and are not re-flagged as blocking, per the standing user decision. Updates: **r4 F-1 is
closed** (three survivors dead, verified by reproduction); **r4 F-2 is two-thirds closed** (table
corrected, Consequences not — F-1 above); r4 F-3 remains open and is now four rows (F-2 above); r1 F-3
("three of the four Tab-activation rows do not discriminate") is now further eroded — M5 kills 5 rows,
up from 4 at r4 and 2 at r3.

## Assessments
- **Requirement satisfaction**: all eight ACs hold, and I established that by driving the shipped code
  myself across 40 scenarios (five button families × `Enter`/`Space` × focused/unfocused, eight
  unmapped/modifier keys with a counting `preventDefault`, five held-key cases, both display lines),
  not by reading the suite's green output. No product file changed since the cycle-3 head, so the
  behavior is bit-for-bit what I approved on behavior grounds at r3 and r4.
- **Test adequacy / false confidence**: the new row is exactly what my F-1 specified — named after the
  behavior, placed in the AC-6/AC-7 block, independent (fresh `loadPage()`), asserting a whole display
  pair rather than a truthy value, and reusing `loadPage`/`typeKeys`/`display`/`assertDisplay` with no
  new helper. It cannot pass vacuously: a stub that failed to activate at all would leave `123`, and a
  stub that over-activated would leave `0` — both are the observed mutant outputs, so the expected
  `12` is pinned on both sides. It is the unique killer of all three targeted mutants. 32 of my 36
  non-equivalent mutants died; of the four survivors, two are measured-equivalent, one is a known
  carried Nit (C5), and two are single-`data-*`-value carve-outs (F-3, F-4). None is a behavior defect.
  No test was weakened, renamed, skipped or deleted.
- **Edge cases missing**: `Tab`-focused `AC` under `Enter` (F-3) and held `Space` on operator/action
  buttons (F-4) — both measured, both Nit. Held `Escape`/`Delete` on the document remains undefined by
  OQ-4 and is idempotent, still correctly listed as "Not covered". `Alt` at the page/wiring level
  remains unit-only (r1 F-6, confirmed by C4). Everything else I could construct is covered or
  provably inert.
- **Regression risk**: none introduced — additive test code only; unit 83, integration 50 (49 + 1),
  regression 25, with no pre-existing test edited, weakened or deleted (`validate.mjs state` OK, no
  `.only`/`.skip`, `git diff --name-status` shows one added-lines-only test file over the cycle).
  Checked behaviors that could have broken and did not: the 20 `dom-click.test.js` rows, the README
  click sequences under the installed blur (`every README click sequence still renders correctly once
  the keyboard listener and click blur are installed`), divide-by-zero including the CALC-001 mid-chain
  case, and the four source-safety scans. The residual risk is future, not present, and is smaller than
  at r4: the `Enter`/`Space`-on-button branch is now pinned for all three button families and for both
  keys on digits, so the class of simplification that shipped a bug in cycles 1–4 is caught.
- **Conventions & complexity**: 2-space indent, single quotes, semicolons, `const`, `camelCase`,
  `node:test` + `node:assert/strict`, a behavior-stating test name, one behavior per test, a comment
  that explains *why* (citing r4 F-1 and the `D-013` row) rather than *what*. Branch
  `agent/KEY-001-keyboard-input`, `KEY-001: …` commit subjects, nothing pushed. No new abstraction, no
  duplication, no production complexity added.
- **Security implications**: test-only change. No `eval`/`new Function`/`innerHTML`, no network, no new
  dependency, nothing written outside the repo. The closed `KEY_MAP` allowlist, the `textContent`-only
  display path and the bounded `preventDefault()` surface are untouched; `mapKey` still returns `null`
  for `constructor`/`__proto__` (it is a `Map`) and for `undefined`/`''` without throwing, which I
  re-verified directly. The formal verdict is `security-reviewer`'s.
- **Performance implications**: no material risk — zero production bytes changed. The keydown hot path
  is unchanged and O(1) per event (two string compares, one dataset destructure, one `Map.get`); the
  click path adds one `blur()` behind an `event.detail` compare. The new row adds 3 synthetic events
  and one page load to a 50-test suite that completes in ~0.5 s locally. Examined and dismissed as
  non-issues: the `dataset` `Proxy` allocation per `isCalculatorButton` call (stub-only, not shipped)
  and the `KEY_MAP` construction (once at load).
- **Architecture fit**: unchanged and sound — pure allowlist and repeat policy in the core, one
  `dispatch(input)` seam in the adapter, native activation left to the browser (`D-005` clause 3,
  `D-013` clauses 1–2). The two independent repeat policies (descriptor-level `allowsRepeat`,
  event-level activation suppression) are now pinned from both sides for every button family. The
  remaining structural observation is F-3 option (b): the branch's guarantee is a *table*, and it is
  currently tested by four hand-picked cells of that table; expressing it as a table-driven test would
  make the correspondence total instead of sampled.

## Unverifiable
- **Real-browser semantics (RK-3)** — no browser here: that `event.detail >= 1` for a pointer/touch
  click and `0` for a keyboard-synthesized activation (`D-011`'s premise); that `preventDefault()` on a
  repeat `Enter` keydown suppresses Chrome/Firefox repeat activation; and that the `Space` guard
  neither is needed nor suppresses the single keyup-driven activation (r3 F-4 — the stub activates on
  keydown for both keys). The manual checklist in `.agent/units/KEY-001.md` (RK-3, items 1–5) covers
  all of these and must be run by the user before this behavior is trusted in production.
- Real OS auto-repeat timing/rate, focus rings, `file://` loading, CSS/layout/theme — outside the stub,
  unchanged from BOOT-001/CALC-001.
- Non-US keyboard layouts (whether a physical key yields the assumed `event.key`) — no AC claims layout
  independence; not a finding.
- Whether `8c36740a` is the SHA the orchestrator will treat as final: I verified it is product-identical
  to `ae9d7c8` and that HEAD drift is `.agent/`-only, but **any new commit invalidates this approval**
  (CLAUDE.md §11).
- Per the dispatch and CLAUDE.md §8 I did not read handoffs `KEY-001-04`, `-06`, `-08`, `-16`, `-24`,
  `-32`, `-40`, nor any implementer/integration-tester justification.
